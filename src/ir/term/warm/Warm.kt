package ir.term.warm

/**
 * Simple type alias to make code clearer.  This allows us to distinguish between the “warm” and “cool” IR in
 * a more readable way:
 *
 * ```
 * Add<Warm> vs. Add<Term>
 * ```
 */
typealias Warm = Term
