package ir.structure.impl

/**
 * Marker interface that represents IR structures, such as IR ops and blocks.
 *
 * It is useful, because it allows us to use generic types that only apply to IR structures.
 */
interface IrStructure