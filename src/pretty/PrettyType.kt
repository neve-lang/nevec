package pretty

import type.Type

/**
 * Pretty-printer helper for types.
 */
object PrettyType {
    /**
     * @param type The type in question.
     *
     * @return a readable name for the type in question.
     */
    fun pretty(type: Type): String {
        TODO()
    }
}